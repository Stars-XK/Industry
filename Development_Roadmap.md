# Development Roadmap

## Unimplemented Features Identified from Specs

### 1. 登录与大屏门户 (Portal)
- **多模态登录**：缺失手机短信验证码、企业微信/钉钉扫码登录模式。
- **GIS/BIM 底座集成**：大屏当前仅为 Echarts 模拟，缺失真实的 WebGL/Three.js 地图底座（高德/天地图/GeoServer 切换）与 BIM 空间透视。
- **安全间距与分析工具**：缺失基于地图的测距与缓冲区分析（动土审批拦截），以及 GIS 爆管关阀寻址与停水影响分析功能。
- **地图联动交互**：缺失发生高优报警时地图自动缩放聚焦（Zoom-in）、节点标红闪烁及联动右侧监控视频抽屉的交互。

### 2. 综合业务监控台 (SCADA & Monitor)
- **水质综合看板补全**：`/scada/overview` 缺失 COD 及其他水质指标的完整图表支持。
- **报警根因状态联动**：`/scada/topology` 拓扑树尚未接入 RCA 引擎，无法在前端有效抑制子报警并仅标红根因节点。
- **全景数字沙盘**：`/scada/security` 缺失将视频流动态贴图至 BIM 模型表面（展示摄像头盲区）的功能。
- **AI 安防联动弹窗**：缺失门禁尾随与火情烟感报警的强制弹窗及现场 15 秒短视频截取功能。

### 3. 多维统计与数据分析 (Data Analytics)
- **时空拓扑切片查询**：产销差报表 (`nrw`) 缺失“拓扑历史版本”时间轴选择框，以及父子分区的穿透下钻（Drill-down）。
- **MNF 高阶分析**：缺失大用户夜间合法用水的手动/自动剥离操作开关；缺失基于水力模型的“声学异常频段热力图”交叉定位覆盖。
- **大户防漏收诊断**：缺失针对“大表小用/水表倒转”的 AI 诊断看板，及水表降级/替换建议单导出功能。
- **营收对账闭环**：缺失本月“应收 vs 实收”资金回款漏斗图，及针对异常用量的稽查追缴单追踪流程。
- **能效与防窃电模型**：`/analytics/energy` 缺失总分表差额防窃电比对模型，以及基于峰谷平电价的“抽水蓄水错峰排班建议表”。
- **在线水力模型云图**：`/analytics/hydraulic` 暂为静态地图占位，缺失与 EPANET 引擎直连的全网节点/管段推演等压云图展示。

### 4. 运维治理与协同闭环 (O&M & Workflow)
- **SCADA 联锁反馈**：报警中心缺失底层 SCADA 联锁动作（如：系统已自动关泵）的同步执行结果展示。
- **工单场景覆盖**：缺失移动端（H5）扫码处理页；缺失“未计费水量（消防/绿化）冲销”的带现场照片专项审批标签页；缺失派单时的 GIS 就近人员推荐下拉框。
- **气象协同与 GIS 追踪**：缺失接入气象局数据的“极端防汛防冻前置工单看板”；缺失人员/车辆在 GIS 上的实时定位与历史轨迹回放。
- **SOP 数字化追踪**：缺失应急突发时强制弹窗的 SOP 操作台，未实现操作员确认每一步时间戳的留痕与回溯功能。

### 5. 数据中台与治理底座 (Data Governance)
- **数据融合**：缺失外部财务系统 Excel 账单的批量导入功能。
- **配方管理进阶**：配方下发缺失带有电子签名的防篡改校验，及多版本配方的回滚机制。
- **日志与监控**：联锁引擎 (`interlock`) 缺失详细的自动动作执行历史流水账列表。

### 6. 系统设置与台账权限 (System & Security)
- **系统主题修改**：系统配置页面 (`config.vue`) 需要增加动态修改系统主题色（Primary Color）和暗/亮模式切换的功能。
- **资产强约束校验**：`/system/asset` 缺失专用的“换表/拆表”业务表单，未强制校验“老表拆除止码”与“新表安装起码”以防止底层时序库出现负流量。
- **仓储预警与成本挂钩**：`/system/inventory` 缺失安全库存红绿灯预警看板；未实现与外勤工单联动的扫码领料出库扣减报表。
- **审计日志深度**：`/system/audit` 操作流水账目前仅记录基础请求信息，缺失针对核心表单修改前后的 JSON Diff 细粒度对比展示。
- **实施级部署能力**：`/system/wizard` 缺失大文件前端切片上传与断点续传逻辑；`/system/backup` 缺失定时自动备份的策略配置面板。
